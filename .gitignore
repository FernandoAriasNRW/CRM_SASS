# ─────────────────────────────────────────────────────────── .NET
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
*.userprefs
project.lock.json
artifacts/

# Visual Studio / Rider
.vs/
.idea/
*.sln.docstates

# Test / coverage output
[Tt]est[Rr]esult*/
coverage/
*.coverage
*.trx

# ─────────────────────────────────────────────────────────── Secretos
# Nunca versionar configuración con credenciales reales.
appsettings.Development.json
appsettings.Local.json
appsettings.*.local.json
*.pfx
*.key
.env
.env.*
!.env.example

# ─────────────────────────────────────────────────────────── Node / Angular
node_modules/
dist/
.angular/
npm-debug.log*
yarn-error.log*

# ─────────────────────────────────────────────────────────── Logs y artefactos de trabajo
*.log
build.log
error.html
error.txt
payload.json
*.webm

# ─────────────────────────────────────────────────────────── SO
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ─────────────────────────────────────────────────────────── Herramientas
# Con `.claude/` a secas git no entra en la carpeta y la excepción de abajo no se aplica.
.claude/*
# Las skills del proyecto sí se comparten: son el estándar de código, no configuración personal.
!.claude/skills/

# Playwright
web/test-results/
web/playwright-report/

# Playwright escribe también en la raíz cuando se le llama desde fuera de `web/`, que es lo que
# pasa al lanzar la suite desde el directorio del proyecto.
test-results/
playwright-report/
web/blob-report/
web/.playwright/
